# - name: install terraform
#   import_playbook: install_tf.yml

- name: terraform apply
  hosts: 127.0.0.1
  connection: local
  gather_facts: false
  vars_files:
    - vars.yml
  vars:
    plugin_dir: ../terraform/.terraform/plugins/linux_amd64
  tasks:
    - name: "{{ terraform_action }}"
      terraform:
        force_init: "{{ force_init }}"
        project_path: ../terraform/ # required
        backend_config:
          region: "us-east-1"
          bucket: "awx-nextlinker-cmdb"
          key: "terraform.tfstate"
        state: "{{ terraform_state }}" # present, absent, planned
        variables:
          webserver_ip: "{{ webserver_ip }}"
